# Datasets (raw data, not tracked — see Dataset_Summary.md for source/how to get it)
ThaiCharacter Dataset/
ThaiCharacter Dataset.zip
datasets for testing/

# Trained model weights (large binaries — retrain via TrainingCNN.py / the Colab notebook to regenerate)
*.pt
*.pth
# ...แต่ น้ำหนักของแต่ละ Round เก็บลง repo ตั้งใจ (โจทย์ให้ส่งไฟล์ Weight)
!/Round */model.pt
!/Round */runs/*/model.pt

# Python
__pycache__/
*.pyc

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Editor (local settings, not shared)
.vscode/

# Planning/progress docs & run logs — kept locally only, not part of the shipped code
/Dataset_Summary.md
/Implementation_Plan.md
/PROGRESS.md
/Project_References.md
/Project_info.md
/Project_info.png
/output_colab.ipynb
/output_colab_round2.ipynb

# Local-run script variants (VSCode/terminal, not needed to pull into Colab —
# the notebook above is self-contained: writes its own Net.py via %%writefile)
/Net.py
/Test/
