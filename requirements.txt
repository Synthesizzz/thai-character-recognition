# Thai Character & Number Recognition — ไลบรารีที่โค้ดใน Round 1-4 import จริง
# ติดตั้ง:  pip install -r requirements.txt
#
# ค่า ">=" คือขั้นต่ำโดยประมาณ ทดสอบจริงเฉพาะเวอร์ชันที่ระบุในวงเล็บ (Python 3.12, Windows) เวอร์ชันอื่นยังไม่ได้ลอง
#
# --- PyTorch (เลือกวิธีติดตั้งให้ตรงกับการ์ดจอ) --------------------------------------------------
# * NVIDIA (CUDA): ต้องลงเวอร์ชันที่มี CUDA ไม่ใช่เวอร์ชัน CPU — ถ้า `pip install torch` เฉย ๆ บางเครื่องได้เวอร์ชัน CPU
#   ให้ติดตั้ง torch/torchvision ตามคำสั่งในหน้า https://pytorch.org/get-started/locally/ ก่อน
#   (เช่น pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121) แล้วค่อย pip install -r requirements.txt
#   และ *ไม่ต้อง* ติดตั้ง torch-directml (สคริปต์ Round 3-4 จะเลือก DirectML ก่อน CUDA ถ้าติดตั้งไว้ ซึ่งช้ากว่า)
# * AMD/Intel GPU บน Windows: เพิ่ม `pip install torch-directml` (ดู GPU_Setup_Guide_AMD.md) ใช้ได้กับ Round 3-4 เท่านั้น
# * ไม่มี GPU: ใช้เวอร์ชัน CPU ได้ (ช้ากว่า แต่ inference 432 ภาพใช้เวลาไม่นาน)
#
torch>=2.0            # ทดสอบกับ 2.4.1
torchvision>=0.15     # ResNet18 pretrained (ทดสอบกับ 0.19.1)
numpy>=1.24           # ทดสอบกับ 2.5.3
scikit-image>=0.21    # imread / resize / rotate / random_noise (ทดสอบกับ 0.26.0)
scikit-learn>=1.3     # train_test_split (ทดสอบกับ 1.9.1)
tqdm>=4.60            # แถบความคืบหน้า (ทดสอบกับ 4.70.1)
